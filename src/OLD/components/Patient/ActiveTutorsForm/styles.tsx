import styled from "styled-components";

export const Container = styled.section`
  h2 {
    margin-bottom: 20px;
  }

  .tutor-box {
    cursor: pointer;
    :hover {
      color: var(--blue);
    }
  }
`;
