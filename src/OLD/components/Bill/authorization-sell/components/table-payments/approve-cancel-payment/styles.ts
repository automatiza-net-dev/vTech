import styled from "styled-components"

export const Cancel = styled("div")`
    display: flex;
    gap: 10px;
    align-items: center;
    padding: 5px 0;

    .quantity {
        width: 60px;
    }

    .input_control {
        margin-bottom: 0;
    }
`