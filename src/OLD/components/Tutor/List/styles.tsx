import styled from 'styled-components';

export const Container = styled.div`
    .add-icon {
        color: var(--darkBlue);
        :hover {
            color: var(--blue);
            cursor: pointer;
        }
    }
`