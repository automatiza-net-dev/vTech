import styled from "styled-components";

export const Container = styled.div`
  .confirm-icon-active {
    color: #3cb371;
    cursor: pointer;
  }

  .vaccine-icon {
    cursor: pointer;
  }

  .cancel-icon-active {
    color: #ff0000;
    cursor: pointer;
  }

  .schedule-active {
    color: 	#008B8B;
    cursor: pointer;
  }

  .icon-inative {
    color: #c0c0c0;
    cursor: not-allowed;
  }

  .custom-input {
    border: 1px solid #bababa;
    height: 32px;
    border-radius: 3px;
  }
`;
