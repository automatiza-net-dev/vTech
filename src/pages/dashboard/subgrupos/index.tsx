import Subgroups from "@/OLD/components/Subgroup";
import { LayoutDashboard } from "@/presentation";

export default function SubgroupsPage() {
  return (
    <LayoutDashboard>
      <Subgroups />
    </LayoutDashboard>
  );
}
