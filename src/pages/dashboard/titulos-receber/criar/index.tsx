import CreateTitle from "@/OLD/components/Titles/Create";
import { LayoutDashboard } from "@/presentation";

export default function CreateTitlePage() {
  return (
    <LayoutDashboard>
      <CreateTitle type={"receive"} />
    </LayoutDashboard>
  );
}
