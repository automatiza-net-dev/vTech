import { Notes } from "@/OLD/components/Notes";
import { LayoutDashboard } from "@/presentation";

export default function NotesPage() {
  return (
    <LayoutDashboard>
      <Notes />
    </LayoutDashboard>
  );
}
