import CashierRegimeReport from "@/OLD/components/CashierRegimeReport";
import { LayoutDashboard } from "@/presentation";

export default function CashierRegimeReportPage() {
  return (
    <LayoutDashboard>
      <CashierRegimeReport></CashierRegimeReport>
    </LayoutDashboard>
  );
}
