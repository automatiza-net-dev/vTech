import { ScheduleStatus } from "@/OLD/components/ScheduleType/ScheduleStatus";
import { LayoutDashboard } from "@/presentation";

export default function ScheduleStatusPage() {
  return (
    <LayoutDashboard>
      <ScheduleStatus />
    </LayoutDashboard>
  );
}
