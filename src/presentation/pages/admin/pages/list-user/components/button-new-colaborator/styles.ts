import styled from "styled-components";

export const ButtonNewCollaborator = styled("div")`

    button {
        font-size: 12px;
        height: 37px;
        background-color: ${props => props.theme.primaryColor};
    }

`